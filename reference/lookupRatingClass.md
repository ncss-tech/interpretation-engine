# Lookup Rating Classes by Rule ID

Lookup Rating Classes by Rule ID

## Usage

``` r
lookupRatingClass(ruleiidref)
```

## Arguments

- ruleiidref:

  Rule ID

## Value

A data.frame containing columns `ruleiidref` (Rule ID),
`ratingclassname`, `ratingclassupperboundary` and `ruleratingclassiid`
