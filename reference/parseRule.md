# Convert interpretation rule into data.tree representation

Convert interpretation rule into data.tree representation

## Usage

``` r
parseRule(x)
```

## Arguments

- x:

  should contain `rule` element with XML text to parse

## Value

a data.tree
