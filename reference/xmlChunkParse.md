# Parse an evaluation chunk XML return a list

Parse an evaluation chunk XML return a list

## Usage

``` r
xmlChunkParse(x)
```

## Arguments

- x:

  evaluation curve XML text

## Value

parsed XML chunk
